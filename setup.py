from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='This repository is my attempt to build and train models.',
    author='karan',
    license='MIT',
)
